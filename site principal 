<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>ChatBoyAI - Bots Inteligentes</title>
  <style>
    :root {
      --primary: #FFD700; /* Dourado */
      --secondary: #1a1a2e;
      --light: #ffffff;
      --dark: #121212;
      --bg-card: rgba(255, 255, 255, 0.05);
      --hover-bg: rgba(255, 255, 255, 0.1);
      --transition: all 0.3s ease-in-out;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', sans-serif;
    }

    body {
      background: url('https://images.unsplash.com/photo-1496715976401-b3cfd5bc6eb2?ixlib=rb-4.0.3&auto=format&fit=crop&w=1470&q=80') no-repeat center center fixed;
      background-size: cover;
      color: var(--light);
      min-height: 100vh;
    }

    header {
      position: fixed;
      width: 100%;
      top: 0;
      left: 0;
      background: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(10px);
      z-index: 1000;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
    }

    .logo {
      font-weight: bold;
      font-size: 1.2rem;
      color: var(--primary);
    }

    nav a {
      color: var(--light);
      text-decoration: none;
      margin-left: 1.5rem;
      font-weight: 500;
    }

    main {
      padding-top: 80px;
      padding-bottom: 4rem;
      background: rgba(0, 0, 0, 0.7);
    }

    .container {
      max-width: 1200px;
      margin: auto;
      padding: 2rem;
    }

    h1 {
      text-align: center;
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    p.center {
      text-align: center;
      max-width: 600px;
      margin: 0 auto 3rem;
      opacity: 0.8;
    }

    .bots-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
    }

    .bot-card {
      background: var(--bg-card);
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      transition: transform 0.3s ease;
    }

    .bot-card:hover {
      transform: translateY(-10px);
    }

    .bot-card.free {
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .bot-card.premium {
      border: 2px solid var(--primary);
      box-shadow: 0 0 10px var(--primary);
    }

    .bot-icon {
      font-size: 3rem;
      color: var(--primary);
      margin-bottom: 1rem;
    }

    .bot-name {
      font-size: 1.5rem;
      margin-bottom: 0.5rem;
    }

    .bot-price {
      font-size: 1rem;
      color: var(--primary);
      margin: 0.5rem 0;
    }

    .bot-description {
      font-size: 0.9rem;
      margin-bottom: 1rem;
      opacity: 0.8;
    }

    .bot-actions button {
      background: var(--primary);
      color: black;
      border: none;
      padding: 0.5rem 1rem;
      margin: 0.3rem;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
      transition: var(--transition);
    }

    .bot-actions button:hover {
      background: #fff; 
    }

    .response-modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.7);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 1001;
    }

    .modal-content {
      background: var(--secondary);
      padding: 2rem;
      border-radius: 12px;
      max-width: 400px;
      width: 90%;
      text-align: center;
      position: relative;
    }

    .close-modal {
      position: absolute;
      top: 10px;
      right: 10px;
      background: none;
      border: none;
      font-size: 1.2rem;
      color: var(--primary);
      cursor: pointer;
    }

    .chat-example {
      margin-top: 1rem;
      padding: 1rem;
      background: #222;
      border-radius: 8px;
      font-style: italic;
    }

    .notification {
      position: fixed;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--primary);
      color: black;
      padding: 1rem 2rem;
      border-radius: 8px;
      display: none;
      animation: fadeOut 2s forwards;
    }

    @keyframes fadeOut {
      from {opacity: 1;}
      to {opacity: 0; visibility: hidden;}
    }

    @media (max-width: 600px) {
      h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>

<header>
  <div class="logo">ChatBoyAI</div>
  <nav>
    <a href="#bots">Bots</a>
  </nav>
</header>

<main>
  <div class="container">
    <h1>Bem-vindo ao ChatBoyAI</h1>
    <p class="center">Escolha seu bot inteligente ideal para atendimento, segurança, tradução e automações.</p>

    <section id="bots">
      <div class="bots-container">
        <!-- Bot Gratuito -->
        <div class="bot-card free">
          <div class="bot-icon">🤖</div>
          <h3 class="bot-name">Bot Gratuito Básico</h3>
          <div class="bot-price">Grátis</div>
          <p class="bot-description">Atendimento rápido e simples para iniciantes.</p>
          <div class="bot-actions">
            <button disabled>Assinar</button>
            <button onclick="showResponse('respostaBasico')">Ver Resposta</button>
          </div>
        </div>

        <!-- Bot Premium -->
        <div class="bot-card premium">
          <div class="bot-icon">💼</div>
          <h3 class="bot-name">Bot de Atendimento Premium</h3>
          <div class="bot-price">R$ 49,99/mês</div>
          <p class="bot-description">Atendimento avançado com IA natural e respostas rápidas.</p>
          <div class="bot-actions">
            <button onclick="subscribe('Premium')">Assinar</button>
            <button onclick="showResponse('respostaPremium')">Ver Resposta</button>
          </div>
        </div>

        <!-- Bot Segurança -->
        <div class="bot-card premium">
          <div class="bot-icon">🛡️</div>
          <h3 class="bot-name">Bot de Segurança para Discord</h3>
          <div class="bot-price">R$ 89,99/mês</div>
          <p class="bot-description">Moderador automático e proteção contra spam e conteúdo impróprio.</p>
          <div class="bot-actions">
            <button onclick="subscribe('Discord')">Assinar</button>
            <button onclick="showResponse('respostaSeguranca')">Ver Resposta</button>
          </div>
        </div>

        <!-- Bot Tradutor -->
        <div class="bot-card premium">
          <div class="bot-icon">🌍</div>
          <h3 class="bot-name">Bot de Tradução Instantânea</h3>
          <div class="bot-price">R$ 149,99/mês</div>
          <p class="bot-description">Traduz conversas em tempo real para mais de 30 idiomas.</p>
          <div class="bot-actions">
            <button onclick="subscribe('Traducao')">Assinar</button>
            <button onclick="showResponse('respostaTraducao')">Ver Resposta</button>
          </div>
        </div>

        <!-- Bot Max -->
        <div class="bot-card premium">
          <div class="bot-icon">👑</div>
          <h3 class="bot-name">ChatBoy Max</h3>
          <div class="bot-price">R$ 299,99/mês</div>
          <p class="bot-description">O melhor da IA: automação total, personalização e recursos exclusivos.</p>
          <div class="bot-actions">
            <button onclick="subscribe('Max')">Assinar</button>
            <button onclick="showResponse('respostaMax')">Ver Resposta</button>
          </div>
        </div>
      </div>
    </section>
  </div>
</main>

<!-- Modal de Resposta -->
<div class="response-modal" id="responseModal">
  <div class="modal-content">
    <button class="close-modal" onclick="hideResponse()">×</button>
    <h3>Exemplo de Resposta</h3>
    <div class="chat-example" id="responseText"></div>
  </div>
</div>

<!-- Notificação -->
<div id="notification" class="notification"></div>

<script>
  function subscribe(plan) {
    showNotification(`Você assinou o plano ${plan}!`);
  }

  function showResponse(botType) {
    const responses = {
      respostaBasico: "Olá! Como posso ajudar?",
      respostaPremium: "Como posso otimizar sua experiência hoje?",
      respostaSeguranca: "Mensagem bloqueada por conter linguagem imprópria.",
      respostaTraducao: "Traduzindo: 'Hello' → 'Olá'",
      respostaMax: "Sistema pronto. Comandos carregados. Pronto para uso completo."
    };

    document.getElementById("responseText").innerText = responses[botType] || "Resposta não disponível.";
    document.getElementById("responseModal").style.display = "flex";
  }

  function hideResponse() {
    document.getElementById("responseModal").style.display = "none";
  }

  function showNotification(message) {
    const notif = document.getElementById("notification");
    notif.innerText = message;
    notif.style.display = "block";
    setTimeout(() => notif.style.display = "none", 2000);
  }
</script>

</body>
</html>